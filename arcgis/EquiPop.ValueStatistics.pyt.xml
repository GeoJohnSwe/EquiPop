<?xml version='1.0' encoding='UTF-8'?>
<metadata xml:lang="en"><Esri><ArcGISFormat>1.0</ArcGISFormat><SyncOnce>TRUE</SyncOnce></Esri><tool name="ValueStatistics" displayname="2. Value Statistics (numeric fields among the k nearest)" toolboxalias="equipop"><summary>Describes TREATMENT fields - income, rent, age - among the k nearest members of the REFERENCE population around every point. Tick the measures you need (mean, median, Gini, sd, variance, se, min, max, count, sum, range, percentiles); only those are computed. With a count field every statistic is weighted by it, so a point standing for forty counts forty times a point standing for one - including the median, the Gini and every percentile. Nv_&lt;field&gt;_k reports how many neighbours had a usable value, so thin coverage is visible rather than hidden.</summary><parameters><param sync="true" name="layer" displayname="Input points (layer) or table" type="Optional" direction="Input"><dialogReference>The points to analyse - a point layer (coordinates are read straight from the geometry) or a plain table with coordinate columns. Coordinates must be metric; degree data is refused unless auto-projection is ticked.</dialogReference></param><param sync="true" name="coordsrc" displayname="Coordinate source" type="Optional" direction="Input"><dialogReference>Auto uses the geometry when the input has any, and attribute fields otherwise. Choose Attribute fields to override, e.g. when a layer carries coordinates in columns you trust more than its geometry.</dialogReference></param><param sync="true" name="xfield" displayname="X field (easting) - tables/attribute mode" type="Optional" direction="Input"><dialogReference>The easting column - only for tables or attribute mode. Guessed when the name is recognisable (X/East/Easting/POINT_X...); no renaming is needed.</dialogReference></param><param sync="true" name="yfield" displayname="Y field (northing) - tables/attribute mode" type="Optional" direction="Input"><dialogReference>The northing column - only for tables or attribute mode.</dialogReference></param><param sync="true" name="refmode" displayname="How is the reference population defined?" type="Optional" direction="Input"><dialogReference>How the reference population is built, from the simplest way upward. 'Every point counts as one' needs nothing else - one row, one thing. 'A field holds the count' is for rows that stand for several people (or guests, or dwellings). 'Only selected types' is for a layer holding many kinds of object where only some belong: eating places among all POIs, say. Boxes that the chosen way does not need are greyed out.</dialogReference></param><param sync="true" name="pop" displayname="Count field - how many each row stands for (empty = one each); k is measured against this" type="Optional" direction="Input"><dialogReference>How many each point stands for - people, jobs, dwellings, services, anything countable. Leave empty when every point counts as one. k counts these, so this field decides how far the k-search must travel - and in Value Statistics every statistic is weighted by it, so a point standing for 40 counts 40 times a point standing for one.</dialogReference></param><param sync="true" name="catfield" displayname="Type field - the column holding the kind of each object (POI type, tenure, country of birth...)" type="Optional" direction="Input"><dialogReference>Build population and groups from the VALUES of one column (codes or names both work) instead of count fields.</dialogReference></param><param sync="true" name="reftable" displayname="Types to INCLUDE in the reference population - one per row, picked from the type field's own values" type="Optional" direction="Input"><dialogReference>Which values of the category field belong to the REFERENCE population - the people or places whose k nearest form each neighbourhood. Leave it EMPTY and every row belongs. This one choice decides what your shares are shares OF: list the eating places and 'fast food' is measured against eating places; leave it empty and the same run measures fast food against every point in the layer.</dialogReference></param><param sync="true" name="keepoutside" displayname="Rows whose type is NOT included" type="Optional" direction="Input"><dialogReference>What happens to a row whose type you did NOT include - a library, when the reference population is eating places. 'Give them results, counting as zero' (the default): the library is nobody's neighbour and changes no one else's numbers, but it still gets its own results, so you can ask what is around the library. 'Leave their results Null': the row is dropped from the run entirely. Note that a row you DID include whose count field is empty behaves like the first case anyway - zero people, still gets results.</dialogReference></param><param sync="true" name="values" displayname="Treatment values - the numeric fields to measure (e.g. income, rent, age)" type="Optional" direction="Input"><dialogReference>The treatment fields - what you measure among the neighbours: income, rent, age. One set of result columns per field. These are AVERAGED over the reference population, never added up, so give values per unit: income per person, not the household total. A column meant to be summed belongs in tool 1.</dialogReference></param><param sync="true" name="measures" displayname="Measures to calculate (none ticked = mean, median, gini)" type="Optional" direction="Input"><dialogReference>Tick the statistics you want; only those are calculated. Leaving every box unticked means the classic trio - mean, median and Gini. Nv_&lt;field&gt;_k always reports how many neighbours actually had a value.</dialogReference></param><param sync="true" name="pcts" displayname="Percentiles (plain numbers, e.g. 10 25 75 90)" type="Optional" direction="Input"><dialogReference>Percentiles as plain numbers, e.g. 10 25 75 90. Used only when 'percentiles' is ticked; results arrive as P10_&lt;field&gt;_k and so on.</dialogReference></param><param sync="true" name="k" displayname="k values" type="Optional" direction="Input"><dialogReference>One or more k values, space-separated (200 1600). Each k gives its own neighbourhood: the nearest k PERSONS, so the radius floats and Dist_k reports it.</dialogReference></param><param sync="true" name="r" displayname="Radii in metres" type="Optional" direction="Input"><dialogReference>Fixed radii in metres, space-separated. The mirror image of k: the area is fixed and the population floats (N_r###).</dialogReference></param><param sync="true" name="existing" displayname="If result fields already exist" type="Optional" direction="Input"><dialogReference>What to do when result fields of the same name are already present: overwrite them, or stop.</dialogReference></param><param sync="true" name="outmode" displayname="Output" type="Optional" direction="Input"><dialogReference>Append results to the input layer, or write a new feature class (recommended for shapefiles: a file geodatabase has no 10-character field-name limit).</dialogReference></param><param sync="true" name="outfc" displayname="New feature class (name/path - use a file geodatabase for unlimited field names)" type="Optional" direction="Input"><dialogReference>Path and name of the new feature class. Put it in a file geodatabase to keep full-length result names - shapefile field names are capped at 10 characters.</dialogReference></param><param sync="true" name="outtable" displayname="Output table (.csv) - for TABLE inputs" type="Optional" direction="Input"><dialogReference>Where a TABLE input's results are written (.csv). The output carries your coordinates plus the result columns, in the original row order.</dialogReference></param><param sync="true" name="unit" displayname="Cell size (m)" type="Optional" direction="Input"><dialogReference>The grid cell size in metres. Bigger cells mean fewer origins and much faster runs; smaller cells mean finer geography. This is the strongest speed control you have.</dialogReference></param><param sync="true" name="selfpot" displayname="Self-potential - the distance to what is LOCAL, inside your own cell" type="Optional" direction="Input"><dialogReference>Self-potential: how far away what is LOCAL - what your own cell already holds, the quantity reported as N_local - is treated as being. Rows are snapped to a grid, so everything in the origin's own cell sits at exactly the origin - distance zero - unless you say otherwise. That matters wherever one cell already contains k of whatever you are counting, which happens in a dense block or at a large cell size: the radius comes out as zero and k stops making any difference, so the nearest 100 and the nearest 1000 give the same answer. Leave this at 1 and the distance is estimated by spreading the cell's contents evenly across it, which recovers the radius you would have measured from individual points to within a fraction of a percent. Set it to 0.71 for the median distance instead of the radius, or to 0 to reproduce results from before this setting existed.</dialogReference></param><param sync="true" name="autoproj" displayname="Auto-project degree data to a suitable metric CRS (layers only - the fitting UTM zone is computed from the data; input untouched)" type="Optional" direction="Input"><dialogReference>When the input is in degrees, project it on the fly to the metric CRS that fits the data (the UTM zone is computed from the extent). The stored data is not modified. Tables cannot be auto-projected.</dialogReference></param><param sync="true" name="shortnames" displayname="Allow shortened field names when the target is a shapefile (10-character cap; names stay collision-free and the mapping is printed)" type="Optional" direction="Input"><dialogReference>Allow result names to be shortened to 10 characters so they fit a shapefile. Names stay unique - no two results ever merge - and the full mapping is printed in the messages.</dialogReference></param></parameters><usage>Give the treatment values, tick the measures, set k. Use the reference population's count field whenever a point stands for more than one - people, jobs, dwellings, services. Gini refuses negative values, and percentiles need numbers in their box. As with machine 1, cell size controls the runtime and a file geodatabase avoids the shapefile name limit.</usage></tool><dataIdInfo><idCitation><resTitle>2. Value Statistics (numeric fields among the k nearest)</resTitle></idCitation><idAbs>Describes TREATMENT fields - income, rent, age - among the k nearest members of the REFERENCE population around every point. Tick the measures you need (mean, median, Gini, sd, variance, se, min, max, count, sum, range, percentiles); only those are computed. With a count field every statistic is weighted by it, so a point standing for forty counts forty times a point standing for one - including the median, the Gini and every percentile. Nv_&lt;field&gt;_k reports how many neighbours had a usable value, so thin coverage is visible rather than hidden.</idAbs></dataIdInfo></metadata>