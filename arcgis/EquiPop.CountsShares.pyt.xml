<?xml version='1.0' encoding='UTF-8'?>
<metadata xml:lang="en"><Esri><ArcGISFormat>1.0</ArcGISFormat><SyncOnce>TRUE</SyncOnce></Esri><tool name="CountsShares" displayname="1. Counts and Shares (k / radius / decay)" toolboxalias="equipop"><summary>Builds an egocentric neighbourhood around EVERY point and counts what is inside it. Two ways to draw it: k (the nearest k persons - population fixed, radius floats, reported as Dist_k) or a radius in metres (area fixed, population floats). Group fields add counts and shares (T_ and R_). Barriers and terrain turn plain distance into EFFORT: rivers, railways, lakes, friction rasters and slopes make neighbours farther away in rounds, and N_tau### counts who is reachable within a budget. Inputs may be point layers (geometry is read directly) or tables with coordinate columns; coordinates must be metric.</summary><parameters><param sync="true" name="layer" displayname="Input points (layer) or table" type="Optional" direction="Input"><dialogReference>The points to analyse - a point layer (coordinates are read straight from the geometry) or a plain table with coordinate columns. Coordinates must be metric; degree data is refused unless auto-projection is ticked.</dialogReference></param><param sync="true" name="coordsrc" displayname="Coordinate source" type="Optional" direction="Input"><dialogReference>Auto uses the geometry when the input has any, and attribute fields otherwise. Choose Attribute fields to override, e.g. when a layer carries coordinates in columns you trust more than its geometry.</dialogReference></param><param sync="true" name="xfield" displayname="X field (easting) - tables/attribute mode" type="Optional" direction="Input"><dialogReference>The easting column - only for tables or attribute mode. Guessed when the name is recognisable (X/East/Easting/POINT_X...); no renaming is needed.</dialogReference></param><param sync="true" name="yfield" displayname="Y field (northing) - tables/attribute mode" type="Optional" direction="Input"><dialogReference>The northing column - only for tables or attribute mode.</dialogReference></param><param sync="true" name="pop" displayname="Population field - total persons at this point (empty if each point is one person)" type="Optional" direction="Input"><dialogReference>Persons represented by each point. Leave empty when every point is one person. k counts PERSONS, so this field decides how far the k-search must travel.</dialogReference></param><param sync="true" name="treat" displayname="Group count fields - persons per group at this point (0/1 if points are individuals)" type="Optional" direction="Input"><dialogReference>Group counts: persons of the group at this point (use 0/1 when points are individuals). Produces T_&lt;group&gt;_k (count) and R_&lt;group&gt;_k (share).</dialogReference></param><param sync="true" name="k" displayname="k values (space-separated, e.g. 200 1600)" type="Optional" direction="Input"><dialogReference>One or more k values, space-separated (200 1600). Each k gives its own neighbourhood: the nearest k PERSONS, so the radius floats and Dist_k reports it.</dialogReference></param><param sync="true" name="r" displayname="Radii in metres (e.g. 500 1000)" type="Optional" direction="Input"><dialogReference>Fixed radii in metres, space-separated. The mirror image of k: the area is fixed and the population floats (N_r###).</dialogReference></param><param sync="true" name="model" displayname="Distance decay" type="Optional" direction="Input"><dialogReference>Distance decay weighting. 'no decay' counts every neighbour equally inside the neighbourhood.</dialogReference></param><param sync="true" name="halflife" displayname="Decay half-life in metres (one value for everybody)" type="Optional" direction="Input"><dialogReference>The distance in metres at which a neighbour counts half as much. Only used when a decay model is chosen.</dialogReference></param><param sync="true" name="hlfield" displayname="OR: half-life from a field - each point keeps its own bandwidth (estimated median distance, group potential...)" type="Optional" direction="Input"><dialogReference>A field giving each point its OWN half-life in metres - an estimated median travel distance, a group-specific potential, whatever you have estimated. Rows are grouped into bandwidth bins and each bin gets its own exact pass.</dialogReference></param><param sync="true" name="hlfromdist" displayname="OR: self-calibrating - use each point's own Dist_k as its half-life (enter the k to calibrate on; urban form sets the bandwidth)" type="Optional" direction="Input"><dialogReference>Self-calibrating bandwidth: enter a k, and each point's own Dist_k - the radius it needed to gather k persons - becomes its half-life. Dense places get sharp kernels, thin places broad ones, with no external estimate.</dialogReference></param><param sync="true" name="hlbins" displayname="Bandwidth bins (variable half-life only; more bins = finer, slower)" type="Optional" direction="Input"><dialogReference>How many bandwidth bins to use when the half-life varies. More bins follow the distribution more closely and cost more passes; distinct values fewer than this get an exact pass each.</dialogReference></param><param sync="true" name="decayeps" displayname="Decay cutoff - ignore weights below this (smaller = wider search = slower; the truncation distance is reported in the messages)" type="Optional" direction="Input"><dialogReference>Where the decayed sum is cut off: neighbours whose weight falls below this are ignored. A decayed sum has no natural edge, so this is what bounds the search. 1e-6 (the default) reaches about 20 half-lives and is slow; 1e-3 reaches about 10 and runs roughly four times faster, with a difference far below any sampling error. The actual distance in metres is reported in the messages.</dialogReference></param><param sync="true" name="catfield" displayname="Category field (codes or names) - builds population and groups from its VALUES" type="Optional" direction="Input"><dialogReference>Build population and groups from the VALUES of one column (codes or names both work) instead of count fields.</dialogReference></param><param sync="true" name="cattable" displayname="Categories: one row per value - which group it belongs to, and whether it counts as population" type="Optional" direction="Input"><dialogReference>One row per category value: which group it joins (leave blank for none) and whether it counts as population. Rows sharing a group name merge into that group - so no separators to remember, and a value can belong to a group WITHOUT being part of the population (services near residents).</dialogReference></param><param sync="true" name="groupscount" displayname="Category groups count" type="Optional" direction="Input"><dialogReference>Whether category groups count PERSONS (weighted by the population field, so shares have the same denominator as N) or PLACES (rows).</dialogReference></param><param sync="true" name="barriertable" displayname="Barriers: one row per point/line/polygon layer or table of cells, with the field holding its friction" type="Optional" direction="Input"><dialogReference>One row per barrier source - a point, line or polygon layer, or a table of cells - with the field holding its friction. Several sources combine per the overlap rule, so a river, a railway and a lake can be given together.</dialogReference></param><param sync="true" name="barrierrasters" displayname="Barrier rasters (cell value = friction); combined with the rows above by the same overlap rule" type="Optional" direction="Input"><dialogReference>Friction rasters, where each cell value is the crossing cost. They combine with the rows of the barrier table by the same overlap rule.</dialogReference></param><param sync="true" name="barrieragg" displayname="Barrier overlap rule (features sharing a cell)" type="Optional" direction="Input"><dialogReference>How several barrier features sharing one cell combine. Additive (the default) stacks costs - a river crossed at a railway costs both. Max/min/mean are available when stacking is wrong.</dialogReference></param><param sync="true" name="dem" displayname="Distance ingredient: elevation raster (DEM)" type="Optional" direction="Input"><dialogReference>Elevation raster: slopes become extra effort, so uphill neighbours are farther away than flat ones.</dialogReference></param><param sync="true" name="tau" displayname="Effort budgets in rounds (e.g. 3 8)" type="Optional" direction="Input"><dialogReference>Effort budgets in rounds, space-separated. With barriers or terrain, N_tau### counts the persons reachable within that many rounds instead of within a plain radius.</dialogReference></param><param sync="true" name="roundtrip" displayname="Round trip (journey home included)" type="Optional" direction="Input"><dialogReference>Count the journey home as well - the budget must cover getting there AND back.</dialogReference></param><param sync="true" name="existing" displayname="If result fields already exist" type="Optional" direction="Input"><dialogReference>What to do when result fields of the same name are already present: overwrite them, or stop.</dialogReference></param><param sync="true" name="outmode" displayname="Output" type="Optional" direction="Input"><dialogReference>Append results to the input layer, or write a new feature class (recommended for shapefiles: a file geodatabase has no 10-character field-name limit).</dialogReference></param><param sync="true" name="outfc" displayname="New feature class (name/path)" type="Optional" direction="Input"><dialogReference>Path/name of the new feature class. Put it in a file geodatabase to keep full-length result names.</dialogReference></param><param sync="true" name="outtable" displayname="Output table (.csv) - for TABLE inputs" type="Optional" direction="Input"><dialogReference>Where a TABLE input's results are written (.csv). The output carries your coordinates plus the result columns, in the original row order.</dialogReference></param><param sync="true" name="unit" displayname="Cell size (m)" type="Optional" direction="Input"><dialogReference>The grid cell size in metres. Bigger cells mean fewer origins and much faster runs; smaller cells mean finer geography. This is the strongest speed control you have.</dialogReference></param><param sync="true" name="autoproj" displayname="Auto-project degree data to a suitable metric CRS (layers only - the fitting UTM zone is computed from the data; input untouched)" type="Optional" direction="Input"><dialogReference>When the input is in degrees, project it on the fly to the metric CRS that fits the data (the UTM zone is computed from the extent). The stored data is not modified. Tables cannot be auto-projected.</dialogReference></param><param sync="true" name="shortnames" displayname="Allow shortened field names when the target is a shapefile (10-character cap; names stay collision-free and the mapping is printed)" type="Optional" direction="Input"><dialogReference>Allow result names to be shortened to 10 characters so they fit a shapefile. Names stay unique - no two results ever merge - and the full mapping is printed in the messages.</dialogReference></param><param sync="true" name="seed" displayname="Random seed (only matters where permutations are used; recorded in the manifest)" type="Optional" direction="Input"><dialogReference>Seed for the parts of EquiPop that use permutations. The counting engines are deterministic; this is recorded in the run manifest so a pseudo-p-value can be reproduced.</dialogReference></param></parameters><usage>Start simple: input layer, one k, nothing else. Add group fields for shares. Add a barrier layer only when barriers matter - it switches the run to the effort engine and takes longer. Cell size is the speed control: doubling it quarters the number of origins. Results are appended to the input unless you choose a new feature class; shapefile targets cap field names at 10 characters, so a file geodatabase is the safer home for long names.</usage></tool><dataIdInfo><idCitation><resTitle>1. Counts and Shares (k / radius / decay)</resTitle></idCitation><idAbs>Builds an egocentric neighbourhood around EVERY point and counts what is inside it. Two ways to draw it: k (the nearest k persons - population fixed, radius floats, reported as Dist_k) or a radius in metres (area fixed, population floats). Group fields add counts and shares (T_ and R_). Barriers and terrain turn plain distance into EFFORT: rivers, railways, lakes, friction rasters and slopes make neighbours farther away in rounds, and N_tau### counts who is reachable within a budget. Inputs may be point layers (geometry is read directly) or tables with coordinate columns; coordinates must be metric.</idAbs></dataIdInfo></metadata>