# EquiPop - Session Handover (v4, lives in docs/ - Claude fetches it)
*Update per release, like MANUAL and BACKLOG. New session = user
writes one line ("continue; next: X"); Claude clones GitHub main,
reads this + BACKLOG.md, and works. Uploads only for private data.*

## State
Python package `equipop` (John Östh, Uppsala; GeoJohnSwe/EquiPop;
PyPI live). Version 1.15.0, 67 tests. MIT, CITATION.cff.
Co-author Umut Türk (Stata) & Marina Toger ; Stata Journal piece planned.

## Doors
- Python (pip) - the tested core, all engines.
- Stata: equipop_knn.ado + equipop_run.ado (counts/stats/friction/
  slope/fca/lisa), sfi-stub validated. UX round SPECCED (net
  install, sthlp help, variable labels) - build on Umut's return.
- ArcGIS Pro: arcgis/EquiPop.pyt - 2 tools ("ingredient" model:
  friction/DEM as distance modifiers on tool 1; category mode;
  counts convention; overwrite/new-output). Field-tested by John.
  ARCGIS_GUIDE.md has the troubleshooting ladder.
- QGIS: future (#7), after ArcGIS family.

## Book (#19)
docs/book/, 14/20 chapters in the approved beginner voice (binding
style guide in BACKLOG), compiled to docx per release (build.sh +
CI artifact). Remaining: 14 areas, 15 maps, 17 bigrun, 18 RunLog,
19 pitfalls/FAQ, 20 Gridby A-Z, appendices. Gridby = the teaching
town (seed 1848, planted truths pytest-enforced); datasets loader.

## Conventions (binding)
One release per conversation; fetch code from GitHub; stage
deliverables progressively; zip + wheel + Book per release; MANUAL
gains version row + validation record + design decisions; BACKLOG
appended; anchors asserted in patches; every engine validated
before shipping (known-answer / cross-engine / stub-verbatim);
loud-by-design everywhere incl. dialogs; writing and coding
sessions separate; closing block: "Next steps & questions".

## Live threads
- Stata-UX (Umut's 3 items + net install) - specced, ready.
- #21d ArcGIS: LISA/FCA tools; features_to_friction Pro wrapper;
  stats-over-effort engine (new research machinery, deferred).
- Awaiting John: estimated propensity M (ch13 recipe) -> rerun
  municipality as research; commuting flows file (half-life
  estimation); Zenodo DOI at 2.0.0 (= Book complete).

## Data notes (short)
Swedish registers RT90 vs SWEREF (value ranges, not names!);
municipality pair anonymised via joint isometry (fixture in
tests/data, results identical to originals); Malta files
re-uploaded on demand; Gridby needs no files.

## John adds
the above is generated by Claude as a sessions transfer; This document will be renewed after each run and function as the main forum for development discussions.
